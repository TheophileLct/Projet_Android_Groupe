package com.example.quizz_app.utils;

import android.app.Activity;
import android.content.Context;
import android.util.Pair;

import com.example.quizz_app.object.Question;
import com.example.quizz_app.object.Quiz;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class QuestionCreate extends Activity {
/*
    List<Pair<String,Boolean>> answers = Arrays.asList(new Pair<String,Boolean>("yep",true));
    Pair<String,Boolean> answer = null;
    ArrayList<Question>questions=new ArrayList<>();
    Question question = null;

    public QuestionCreate(){

    }

    public ArrayList<Quiz> createquestion(){
        ArrayList<Question>questions=new ArrayList<>();

        questions.add(new Question(0,"one piece",answers,20));
        questions.add(new Question(0,"Final fantasy",answers,20));
        questions.add(new Question(0, "retour vers le futur", answers, 20));
        questions.add(new Question(0, "star wars", answers, 20));
        questions.add(new Question(0, "marvel", answers, 20));
        questions.add(new Question(0, "assasins creed", answers, 20));
        ArrayList<Quiz> quiz=new ArrayList<>();
        quiz.add(new Quiz("one piece",questions));
        quiz.add(new Quiz("final fantasy",questions));
        quiz.add(new Quiz("retour vers le futur",questions));
        quiz.add(new Quiz("star wars",questions));
        quiz.add(new Quiz("marvel",questions));
        quiz.add(new Quiz("assasins creed",questions));
        return quiz;
    }



    public ArrayList<Quiz> createListQuiz(){
        //QuestionsService.loadListQuestions(this);
        ArrayList<Question> quiz = new ArrayList<>();
        List<String> name = new ArrayList<String>();
        //name=QuestionsService.getNameOfQuestionLists();
        return null;
    }

    public Context getContext(){
        return this.getContext();
    }*/
}
